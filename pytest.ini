; Pins pytest's rootdir to the repo root regardless of invocation cwd, so the
; root conftest.py (which puts this directory on sys.path) is always collected
; — without it rootdir falls back to cwd and `cd tests && pytest` fails
; collection with ModuleNotFoundError.
[pytest]
testpaths = tests
